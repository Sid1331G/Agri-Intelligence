# Production backend URL — set this to your actual Render service URL.
# Must NOT have a trailing slash. Vercel will bake this into the build bundle.
VITE_API_URL=https://agri-backend-r8x0.onrender.com
